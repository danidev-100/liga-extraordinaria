# Database (PostgreSQL en Neon, Supabase, etc.)
DATABASE_URL="postgresql://user:password@host:5432/liga?schema=public"

# Auth (generar con: openssl rand -base64 32)
AUTH_SECRET="your-auth-secret-here-min-32-chars"

# App URL (dominio de produccion en Vercel)
AUTH_URL="https://tu-app.vercel.app"
